<header id="site-header">
  <div class="nav-shelf">
    <nav role="navigation">

      <ul class="main-nav">
        <li><%= link "Fruits", to: Routes.category_path(@conn, :show, "fruits") %></li>
        <li><%= link "Vegetables", to: Routes.category_path(@conn, :show, "vegetables") %></li>
      </ul>

      <ul class="session-nav">
        <%= if @customer do %>
          <li class="dropdown">
            <a href="#" id="user-nav-toggle">
              <%= @customer.name %>
              <span class="caret"></span>
            </a>
            <ul id="user-nav-options">
              <%= if function_exported?(Routes, :live_dashboard_path, 2) do %>
                <li><%= link "LiveDashboard", to: Routes.live_dashboard_path(@conn, :home) %></li>
              <% end %>
              <li><%= link "Log Out", to: "/logout" %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link "Register", to: Routes.registration_path(@conn, :new), class: "nav-link" %></li>
          <li><%= link "Sign in", to: Routes.session_path(@conn, :new), class: "nav-link" %></li>
        <% end %>
      </ul>
    </nav>

    <a href="/" class="site-logo">
      <img src="<%= Routes.static_path(@conn, "/images/logo.png") %>" alt="Mango Logo"/> Mango
    </a>
  </div>
</header>
